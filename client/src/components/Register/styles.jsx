import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    form: {
        marginLeft: '25%',
    },
    inputs: {
        marginTop: '2px',
        marginBottom: '2px',
    },
});

export default useStyles;
