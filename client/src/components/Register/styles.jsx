import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    form: {
        marginLeft: '25%',
        marginTop: '50px',
    },
    inputs: {
        marginTop: '2px',
        marginBottom: '20px',
    },
});

export default useStyles;
